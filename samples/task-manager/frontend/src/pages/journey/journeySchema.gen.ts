// AUTO-GENERATED — do not edit. Sync from VeloIQ extension schema pipeline.
// Journey definitions and instances live in JSON config files, not the database,
// and the journey list endpoint returns a {data,total} envelope that is NOT a
// standard CRUD resource. The journey experience is delivered through custom
// React bundles (journey launcher / builder) reached from the user menu — it is
// intentionally NOT a schema-driven list resource and does NOT appear in the
// left navigation. Keep this model list empty.
import type { ModelDef } from '@juicemantics/veloiq-ui';

export const journeyModelsGen: ModelDef[] = [];

export default journeyModelsGen;
