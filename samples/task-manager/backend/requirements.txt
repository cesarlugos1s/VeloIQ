# For SQLite (default, no extra driver needed):
safemantiq-framework

# For PostgreSQL, use this instead:
# safemantiq-framework[postgres]

# Auth dependencies (JWT + bcrypt password hashing)
python-jose[cryptography]>=3.3
bcrypt>=4.0
